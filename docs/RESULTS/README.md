# Results Archive

This directory stores benchmark and integration measurement records for
`sc-transport`, including negative or inconclusive outcomes.

Use `template.md` for new entries and keep measurement inputs reproducible.
