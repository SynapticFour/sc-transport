# sc-transport production — copy to .env
SCT_VERSION=v0.1.0

# Host ports (defaults match sct.toml.example)
SCT_LISTEN_PORT=7272
SCT_API_PORT=7273
SCT_METRICS_PORT=9090

# Config file path (mounted into container)
SCT_CONFIG=./sct.toml

DOCKER_PLATFORM=linux/amd64
